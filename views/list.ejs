<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>To do list</title>
</head>
<body>
    <div class = "box" id = "heading">
        <h1> <%= listTitle %> </h1>  
    </div> 
    <div class = "box">
            <% for (i = 0; i < newListItems.length; i++){ %>
                <form action ="/delete" method = "post" >
                    <div class = "item">
                        <input type = "checkbox" onchange="this.form.submit()" name="checkbox" value = "<%= newListItems[i].id%>">
                        <p> <%= newListItems[i].name %></p>
                    </div>
                    <input type="hidden" name = "listNamef" value = "<%=listTitle%>"></input>
                </form>
           <% } %>
           <form class="item" action="/" method="post">
            <label for = "Input">Enter Item</label>
            <input id = "Input" type="text" name="Item" placeholder="New item">
            <button type="submit" name="list" value = <%=listTitle %>>+</button>
        </form>
    </div>
</body>
<footer>
    Copyright the App Brewery
</footer>
</html>